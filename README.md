# Super Simple Chat Tool (SSCT)

## To Start

Open your command line of preference and run to install dependencies
```javascript
npm install 
```

Finally, to start Node & Compile TypeScript run
```javascript
npm run dev
```